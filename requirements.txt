# MedKnow — quick-setup dependency list (core only)
# ============================================================
# Preferred install: pip install -e ".[dev]"  (source of truth: pyproject.toml)
# This file exists for quick environment setup and demo deployment.

# Keep aligned with environment.yml and CI's CPU smoke-test environment.
torch==2.5.1
torchvision==0.20.1
numpy>=1.24
pandas>=2.0
matplotlib>=3.7
scikit-learn>=1.3
pillow>=10.0
pyyaml>=6.0
tqdm>=4.66
grad-cam>=1.5.5

# Optional extras (install only if you use them):
# - api/ FastAPI service: fastapi>=0.110, uvicorn>=0.29, python-multipart>=0.0.9
# - DICOM support: pydicom>=2.4
# - HF Space demo: see hf_space/requirements.txt (gradio)
