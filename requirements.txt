# ========================================================
# 肺炎X光AI辅助诊断系统 — 依赖清单
# ========================================================

# --- 深度学习核心 ---
# ⚠️ 注意：PyTorch 官方 pip 在 Windows 上默认安装 CPU 版！
# 本机请务必使用 conda 环境 lymph_yolo（torch 2.5.1+cu121，CUDA 版）：
#   C:\miniconda3\envs\lymph_yolo\python.exe
# 训练前验证：python -c "import torch; print(torch.cuda.is_available())"  # 应为 True
# 若需手动装 GPU 版：pip install torch==2.5.1 torchvision==0.20.1 --index-url https://download.pytorch.org/whl/cu121
torch>=2.0
torchvision>=0.15
numpy>=1.24
pandas>=2.0

# --- 可视化 ---
matplotlib>=3.7
seaborn>=0.12

# --- 机器学习工具 ---
scikit-learn>=1.3
pillow>=10.0

# --- Web 应用 ---
streamlit>=1.30

# --- 可解释性 AI ---
grad-cam>=1.5

# --- API 服务 ---
fastapi>=0.110
uvicorn>=0.29
python-multipart>=0.0.9

# --- 数据库 ---
# sqlite3 为 Python 内置，无需额外安装

# --- DICOM 支持 ---
pydicom>=2.4

# --- 配置管理 ---
pyyaml>=6.0

# --- 数据处理 ---
kagglehub>=0.2

# --- 训练监控 ---
tensorboard>=2.21.0

# --- 进度条 ---
tqdm>=4.66

# --- 报告生成 (PDF) ---
reportlab>=4.0
